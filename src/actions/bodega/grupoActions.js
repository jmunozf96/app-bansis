export const addGrupoFormAction = (state) => {
    return {
        type: 'ADD_GRUPO_FORM',
        payload: state
    }
};

export const clearGrupoFormAction = (state) => {
    return {
        type: 'CLEAR_GRUPO_FORM',
        payload: state
    }
};